name: System Delivery Payments Service

on:
  push:
    branches:
      - main
    paths:
      - 'payments-service/**'
      - '.github/workflows/payments-service-prod.yml'

jobs:
  build:
    runs-on: self-hosted

    defaults:
      run:
        working-directory: ./payments-service

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup java
        uses: actions/setup-java@v3
        with:
          distribution: temurin
          java-version: '21'
          cache: 'maven'

      - name: Build project
        run: mvn clean install

      - name: Login docker
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
        working-directory: .

      - name: Build docker image
        run: docker build -t caiquepirs/payments-service .

      - name: Publish image
        run: docker push caiquepirs/payments-service

  deploy:
    needs: build
    runs-on: self-hosted
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Create file .env
        run: |
          echo "${{ secrets.DOCKER_ENV_FILE }}" > .env

      - name: Pull docker image
        run: docker pull caiquepirs/payments-service

      - name: Run docker container
        run: |
          docker compose up -d --no-deps payments-service
          docker image prune -f